# === monorepo: dependencias y artefactos ===
**/node_modules
/coverage
*.tsbuildinfo
portfolio-frontend/.next
portfolio-frontend/out
portfolio-api/dist
infra/deploy-api/node_modules

# === secretos (NUNCA subir) ===
.env
.env.*
!.env.example
portfolio-api/.env
portfolio-api/.env.production
portfolio-frontend/.env.production
portfolio-frontend/.env.local
infra/deploy-api/.env

# === uploads en runtime ===
portfolio-api/uploads

# === media pesada (se sincroniza por rsync a la VM, no a git) ===
portfolio-frontend/public/portafolio.glb
portfolio-frontend/public/projects/**/*.mp4

# === mockups de diseño (se guardan local, no en git) ===
stitch_ao_full_stack_portfolio*

# === legacy CRA (eliminado, ignorar si aparecen localmente) ===
/src
/build
/public

# === misc ===
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
infra/deploy-api-server.current.js
